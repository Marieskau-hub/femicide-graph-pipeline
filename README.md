# femicide-graph-pipeline

Graph-based computational framework for representing intimate partner femicide (IPF) cases as heterogeneous knowledge graphs suitable for GNN classification.

Developed as part of an MSc Data Science thesis at IT University of Copenhagen, in collaboration with Center for Voldsforebyggelse (CFV).

**Dataset:** STAT_FW (not included - contact CFV)

## Files

| File | Description |
|------|-------------|
| `network_singlecase_semantic_v7-2-3_gnn-2.ipynb` | Main pipeline notebook. Builds a single case graph from STAT_FW.xlsx and runs a GNN prototype forward pass. |
| `feature_standardiser_v7_adapted.py` | Two-pass feature standardiser. Discovers `rf_*` fields, builds vocabularies, encodes cases to fixed-length float vectors for multi-case GNN input. |
| `build_heterodata_v7.py` | Multi-case HeteroData builder. Loops over all cases in STAT_FW.xlsx and produces a PyG HeteroData corpus object. |
| `network_singlecase_semantic_v6.ipynb` | Historical reference. v6 schema with factor nodes (star topology, pre-P9). |
| `v7_graph_v7-2-3.xlsx` | Schema specification: node types, edge types, attributes, rf_* keys. |
| `v7_documentation.xlsx` | Full documentation: design principles P1–P9, lookup tables, function reference, v5→v6→v7 evolution. |

---

## Schema version: v7-2-3

The v7 schema implements nine design principles (P1–P9):

| # | Principle | Core rule |
|---|-----------|-----------|
| P1 | No double representation | No incident nodes |
| P2 | Dyadic primacy | All O↔V relations as direct edges |
| P3 | Artefact mediation | Weapon/method/location on dyadic path (descriptive only) |
| P4 | No target leakage | No KILLED edge; no outcome-stage data in predictive variant |
| P5 | Two explicit variants | `GRAPH_VARIANT = "descriptive"` or `"predictive"` |
| P6 | Three-state encoding | `value` ∈ {0, 0.5, 1} + `observed` ∈ {0, 1} on every binary |
| P7 | Explicit boundary criterion | Dyadic edge iff both persons required |
| P8 | Deterministic, idempotent | SHA1 edge IDs; sorted rf_* keys |
| P9 | No star topologies | Risk factors as `rf_*` node attributes, not leaf nodes |

---

## Quick start

```python
import pandas as pd
# 1. Open network_singlecase_semantic_v7-2-3_gnn-2.ipynb in Jupyter / Colab
# 2. Set CASE_ID and DATA_PATH in Cell 1
# 3. Run all cells
# 4. Cell 12 exports GEXF + CSV to exports_v7_predictive/
# 5. GNN section runs a prototype forward pass
```

### Running in Google Colab

```python
from google.colab import files
uploaded = files.upload()  # upload Stat_FW.xlsx

# Then open the notebook and run normally
```

---

## Graph variants

**Descriptive** — full case graph including artefacts, timeline, court. For visualisation and analysis.

**Predictive** — prior-stage only. No outcome data. For GNN classification. Excludes:
- Weapon / method / location nodes
- Timeline events (crime, report, verdict)
- Court node
- `SEXUAL_VIOLENCE` edge (during-stage)
- `WITNESSED_CRIME` edge (during-stage)

---

## Node types (predictive variant)

| Type | Count per case | Key attributes |
|------|---------------|----------------|
| `case` | 1 | case_type, case_solved, type_of_femicide |
| `person` (offender) | 1 | age, gender + 19–21 `rf_*` risk factor attrs |
| `person` (victim) | 1 | age, gender + 9 `rf_*` risk factor attrs |
| `person` (child) | 0–N | role=child |

## Feature vectors (predictive variant)

| Node | Formula | Dimension |
|------|---------|-----------|
| Offender | 1 (age) + 19 rf_keys × 2 | **39** |
| Victim | 1 (age) + 9 rf_keys × 2 | **19** |

---

## Dependencies

```
pandas
openpyxl
networkx
torch
torch_geometric
```

---

## Notes

- `Stat_FW.xlsx` is not included in this repository (sensitive data — contact CFV)
- The `exports_v7_*/` directories are generated by running the pipeline notebook
- `build_heterodata_v7.py` is designed for multi-case scaling; the thesis prototype uses a single case
